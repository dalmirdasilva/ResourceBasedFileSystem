#ifndef __RBFS_SPEC_HELPER_H__
#define __RBFS_SPEC_HELPER_H__ 1

#include <rbfs.h>

void resource_dump(rbfs_resource_t *resource);
void format_all();
char* itob(int i);
void rbfs_io_memory_dump(rbfs_t *rbfs);

#endif // __RBFS_SPEC_HELPER_H__
